# Secrets
.env
.env.*
!.env.example

# Build and IDE files
**/target/
.idea/
*.iml
# Local docs (design notes, DB scripts). Kept on disk, not pushed.
docs/
logs/
# JDK do render-jdk.sh tai ve khi build tren Render (khong commit).
.jdk/
jdk.tgz
# Script dev local (dev-up.ps1, test-api.ps1...) va thu muc tools: giu tren may, khong push.
*.ps1
tools/

# Local review scratch and generated test evidence
.scratch_review/
*-test.log
*-final.log
live-*.body
live-*.headers
live-*.json
lifecycle-*.body
lifecycle-*.headers
lifecycle-*.json
cleanup-*.body